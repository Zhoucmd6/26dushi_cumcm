# C题编程工作准则

依据：用户于2026年9月11日粘贴的小红书代码手备赛笔记正文，以及用户要求后续基本参考其工作细节与方法论。本文件将其中的经验适配为当前C题的执行约定；C题的模型选择、实验设计和数据口径说明是本项目的具体应用，不应归称为原作者逐项提出的方案。

当前核心要求：建立可解释的基础方案，逐步验证，再扩大规模；让代码、数据、结果、图表和论文结论能够相互核对。模型或实现存在问题时记录并反馈，不把可疑结果直接用于论文。

## 1 环境与工具

- 主力语言为Python。优先复用已验证可用的环境，不在比赛中无必要切换语言或整体升级依赖。
- 能读懂建模手提供的MATLAB矩阵运算和公式实现；迁移时核对索引、矩阵维度、广播、逐元素运算和求解器约束方向。
- 每个被采用的算法模块都要说明输入、输出、参数、适用条件和失败状态。引用外部或AI生成的代码前，必须理解其实际计算过程。
- 工具按需求选择，不因笔记列举而安装或采用全部算法。当前优先准备时间序列数据处理、规划求解、运行模拟、误差与费用评价和绘图。
- 笔记粘贴中的openpyxltqdm应拆分理解为openpyxl和tqdm；sklearn是导入名，对应常见发行包名scikit-learn。torch、TensorBoard等只在确定采用相应模型时考虑。

## 2 模型转代码前先完成交接

对照《模型交接清单.md》和《架构图与变量口径.md》，统一变量、单位、计量侧、初末条件、各时刻可用的信息、目标函数和约束。

对于尚未明确的调减退款、时段标签、未来电价可知性等，不把临时猜测悄悄固定为正式模型。若为试算采用假设，要在配置和结果说明中标明，并与正式结果区分。

## 3 从基础方案逐步扩大

1. 用2—6个时段的手工可核对场景验证模型实现。使用10分钟步长时保持相应单位，不为调试而无说明改变效率或功率限制。
2. 核对输入输出维度、日期和时段键，观察购电、充放电、储电量及费用中间量。
3. 运行完整单日144个时段，验证第一问的首尾储量及其他约束。
4. 测试跨日衔接和少量不同日期；试运行的历史数据范围按真实决策时间限制。
5. 稳定后运行题目要求的全部日期，并自动生成结果与检查记录。

小样例优先覆盖：无光伏、电池接近上下限、无可用放电余量、光伏富余、预测造成缺电，以及仅在相应问题适用的首尾储量相等条件。这些例子检查的是物理与业务行为，不只检查函数能否调用。

随机算法固定并记录种子；确定性求解不必为了形式强行加随机种子。随机算法需要研究稳定性时使用多种子重复，报告分布，不只保留最优一次。

## 4 结果检查是正式流程的一部分

每次用于比较或交付的运行均需检查：

- 量纲和数量级是否合理，kW与kWh是否混用。
- 母线电量收支、电池储量更新、储量上下限、充放电功率限制是否满足。
- 适用的同时充放电、弃光范围、初始和期末条件是否落实。
- 日期衔接、发布批次和实际执行状态是否正确。
- 分项费用能否独立重算为总费用，是否重复计算购电或调整量。
- 决策、训练、插值和参数估计是否使用了当时尚不可获得的信息。
- 求解器是否成功，返回的是最优解、容差内的解、超时可行解还是失败状态。混合整数问题如可获得界和gap，应记录它们。

负值是否错误取决于变量定义。例如负的“负载−光伏”表示富余电力，不能因负号自动删除。检查可行性时按单位和数值精度设置容差，不对浮点残差要求逐位为零。

发现异常先区分数据、公式、程序和求解器原因。不能通过放宽题设约束、删除困难样本或改写实际值来制造正常结果。发现模型假设有问题时，向建模手说明触发条件、异常时段和证据。

## 5 版本和运行记录

- 原始数据、派生数据、代码、正式结果、图和日志分开保存。当前已有的data/raw、data/processed、src、reports和outputs继续使用。
- 重要代码版本保留可恢复的记录；如项目使用Git，记录提交标识；否则保存明确版本的代码副本。不要只保留不断覆盖的main.py。
- 每次正式或用于比较的实验有唯一run_id。模型版本、数据版本、关键假设、参数和输出必须能通过它关联。
- 新实验按outputs/runs/<run_id>/分开保存；各实验保留config.json、summary.json、daily_results.csv、trajectories.npz和run.log。本次完整运行已归档到05_复核记录，正式表格和图位于04_正式成果。
- config应记录问题编号、方法、模型版本、数据摘要、日期范围、预测窗口、种子（适用时）、求解器及关键设置、时间和计量口径。
- 结果中记录运行时间、求解状态和关键检查；失败运行也留痕，不能只保存成功实验。
- 题目要求的result1.xlsx等固定文件名在最终导出时使用，同时保留对应实验版本；不能让固定交付文件名破坏历史追踪。

探索性报告可以通过脚本重新生成，但一旦用于论文或方法比较，应保留对应版本。修改模型后同步重算受影响的结果和图，避免论文数据来自不同版本。

## 6 同条件比较方法

当前C题可优先考察的比较：简单历史预测与改进预测、无储能或简单储能规则与优化调度、固定计划与预报更新后的调整策略。采用线性规划、混合整数规划或其他方法，取决于模型结构；不预先要求使用粒子群、遗传算法或强化学习。

比较时保持实际测试日期、初始储量、费用口径、可用信息和物理约束一致；每个方案的跨日状态独立连续模拟，不能在各日免费重置。标明计算资源或时间限制。理想全知参考单独标注，不把它作为可执行策略。

同时报告费用、紧急购电量、可行性和必要的运行时间。预测误差更小不自动等于购电成本更低。

方法差异很大时检查实现和实验口径，但合理方法本身也可能存在很大性能差异；不能为了让结果接近而调整、挑选或删除结果。

## 7 有目的地调参与做灵敏度分析

- 调参先固定其他条件，只改变一个明确因素；需要研究交互作用时再设计多因素实验。
- 先提出问题，再选择参数。例如备用水平增加是否降低补电成本、预测窗口变化是否影响效果、设备容量变化能否改变策略。
- 题设参数在正式主结果中保持原值。改变容量、效率、功率限制或补电倍率的实验作为单独的假设场景，不替代题目答案。
- ±5%、±10%、±20%是可选扰动尺度，不是所有参数必须机械采用。例如0.9的效率不能增加20%变成1.08；有概率、非负、容量和整数约束的参数必须保持合法。
- 随机模型比较参数影响时控制随机性；报告关键结论、目标值和可行性是否改变，而不只给一张曲线。
- 从历史数据估计的参数只能利用当时允许的历史窗口，不能用全年真实结果反向挑出最优参数再宣称逐日预测有效。

## 8 图表服务于结论

- 数据结果图直接从可追踪的计算输出生成，优先使用Python；参数或模型改变后能重新出图。
- 概念图、结构图和流程图选能清楚表达关系的工具，可以是PPT、矢量绘图工具或Mermaid；不把某个软件作为硬性要求。
- 正式图统一同类画布尺寸、字体、字号、线宽和变量配色。当前负载用蓝色、光伏用橙色、电价用紫色，可作为后续默认约定。
- 图例位置以不遮挡数据为前提，同类图尽量一致。功率、电量、电价等不同单位明确区分。
- 同时保留作图数据和脚本；正式投稿图根据版面优先使用矢量格式，位图按最终印刷尺寸准备足够分辨率（通常至少300 dpi）。探索图无需为了格式反复重做。
- 每张图交付时带一句它证明的结论，以及样本日期、单位和实验版本。减少无信息装饰，不用复杂图形代替证据。

## 9 与队友的交付方式

收到模型后，先反馈“我如何理解变量和计算流程”，再实现和验证。遇到会影响结果的口径分歧及时说明。

每轮向建模手和论文手交付至少包括：

1. 本轮解决的问题、方法及采用的假设。
2. 代码/运行版本、数据范围和参数。
3. 核心结果、约束检查和失败情况。
4. 对比方案和效果差异，以及差异的合理解释或待查原因。
5. 可直接使用的图表和一句结论，说明正文/附录建议。
6. 未解决事项和下一轮只准备改变的因素。

## 10 在当前项目中的进度

已完成：输入副本与来源摘要、数据类型和结构检查、描述性统计、预报版本整理、初步数据概览图、模型交接清单与概念架构图说明。

新增完成：前两问模型交叉验证；参数化第一问LP/MILP；第二问整体MILP与Benders；因果历史预测和配对样本外残差；8组334天连续实验；20项手算、边界、信息与一致性检查；结果模板、图表及运行记录。用户已授权首版采用建议边界与固定全天充放电计划。

后续扩展：更多预测算法、48小时滚动/更精细终端价值、实时电池响应及第三四问。当前基线不代表这些扩展已比较完毕，也不代表已证明所有因果策略中的全局最优。最新文件和计算口径见《复现与交付指南.md》。
