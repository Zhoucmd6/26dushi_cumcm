# 精简复核记录

`full_v2_20260912` 保留正式运行的最终结论：完成状态、配置与选择、各实验汇总、独立审计、原始数值源码与输入哈希、Excel读回和图形检查。`单元测试.txt` 是原交付测试记录。

NPZ轨迹、预测缓存、完整逐日诊断、源码副本和Excel检查截图已移至本地精简前归档。旧审计中的路径与快照引用用于记录当时运行，不代表本目录仍包含所有中间文件。审计脚本应针对新生成的完整 `outputs/<运行名>` 使用。

`精简清单.json` 记录保留/移出清单和字节数；`精简验证.json` 与 `精简后单元测试.txt` 记录本次交付的核验，不冒充重跑原4837个记录。正式数值结果未重新选模或改参。
