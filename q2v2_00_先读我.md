# 前两问 version2

本版根据建模手的 `model-q1-q2-revised.pdf` 实现前两问，重点补齐第二问的预测候选、整日联合残差有放回抽样、1月预热和历史验证。原工程、第一版成果和第三问成果保留在上一级目录。本次没有改写第三问模型。

目录顺序：

- **01_模型说明**：建模手PDF、本版实现选择与复现说明。
- **02_计算工程**：`src` 代码、`configs` 参数、`data/raw` 原始附件、`data/processed` 规范化数据、`tests` 测试。
- **03_结果**：正式Excel、模型落实报告、指定日期表格、逐时轨迹、对比结果、科研图表。
- **04_复核记录**：完整数值运行、1月验证、随机种子与场景索引、独立审计、测试记录和代码哈希。

建议先阅读 `03_结果/前两问version2模型落实与求解报告.md`，再查看 `03_结果/result2.xlsx`。第一问主结果沿用相同模型重新求解，并补做效率敏感性。

第二问的 (Q,C,D) 仍然在每日零点一次确定；只有紧急购电 (E) 与总未利用电量 (U) 随实际情况产生。终端价值只参与规划，不计入实际电费。

时间区间沿用此前明确采用的“源标签为区间终点”解释，输出从00:00-00:10至23:50-24:00。该口径与PDF§1.1最初的模板标签描述有差异，报告已列明，论文时间定义必须保持一致。

数值主入口为 `02_计算工程/src/run_v2.py`。复现请见 `01_模型说明/实现选择与复现.md`；每次正式修改后使用新运行名保存结果。
