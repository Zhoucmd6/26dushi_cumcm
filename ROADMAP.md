# 建模准备路线图

## 目标与边界

本路线图只覆盖题意理解、数据结构核对、建模方案、补学指南和参考文献。暂不编写求解代码，不计算或填写结果，不撰写论文正文。

## P0 协作基线

交付物：`AGENTS.md`、`ROADMAP.md`。

验收标准：

- 明确只做建模准备，不把代码、数值结果和论文列为当前交付物。
- 明确原始题面、附件和模板只读。
- 统一时间、单位、符号、假设登记和 Git 协作规则。
- 初始化前状态已记录；后续已按用户明确授权建立Git并保留远端历史，不擅自改写历史。

## P1 题意简述与数据结构核对

交付物：`docs/problem-summary.md`、`docs/assumptions.md`。

验收标准：

- 用“已知信息 - 决策 - 约束 - 目标 - 输出”简述四问。
- 核对附件 1 至 4 和附件 5 的工作表、日期、144 个日内数据点及单位。
- 指出数据时间戳与结果模板时段标签存在的 10 分钟歧义。
- 列出所有会改变模型含义的未决口径，包括 0:00 可见信息、储能效率、跨日状态、弃光、调整结算和电价预知。
- 不给出尚未求解的费用、节省率或最优策略数值。

## P2 文献包与补学地图

交付物：`literature/`、`literature/README.md`、`docs/learning-guide.md`。

验收标准：

- 至少下载 5 篇合法公开且能打开的高相关全文。
- 文献覆盖微网储能调度、滚动时域优化、预测不确定性和两阶段结算。
- 每篇文献注明对应问题、阅读优先级和可直接借鉴的建模部件。
- 补学内容按“必须、建议、进阶”分级，并给出中英文检索词和最低学习目标。
- 清楚说明哪些课内知识可以复用，哪些方法需要新学，哪些复杂方法目前可以不学。

## P3 公共模型骨架

交付物：`docs/modeling-ideas.md` 中的公共变量、物理约束、目标和信息时间线。

验收标准：

- 所有功率均先转换为单时段电量，量纲闭合。
- 电力平衡、储能状态转移、容量/功率限制、初末状态、弃光和购电非负性完整。
- 说明何时连续 LP 已足够，何时需要二进制变量防止同时充放电。
- 用一张统一框架解释四问如何逐层增加预测、调整和波动电价。
- 给出概念性验证项：能量守恒、边界、费用拆分和无未来信息泄漏。

## P4 四问建模方案

交付物：`docs/modeling-ideas.md` 的四问分节。

### 问题 1 验收

- 明确采用确定性 LP/MILP、周期末储能约束和购电费用目标。
- 已在 `docs/model-q1-q2.tex` 中列全参数、决策变量、单位、取值域、目标函数和逐类约束。
- 同时给出 LP 与 MILP 版本，并用费用、求解时间和同时充放电诊断量说明选择依据；无数值证据时不宣称二者必然等价。
- 解释如何从 144 个时段汇总题目指定结果。
- 指出效率口径和同时充放电的处理方式。

### 问题 2 验收

- 利用“附件有 1 月数据、结果从 2 月开始”这一结构，提出严格的滚动日前预测，不使用当天未来实际值。
- 已在 `docs/model-q1-q2.tex` 中给出两阶段随机模型、场景生成、实际回放和跨日价值的完整公式。
- 比较历史平均、SARIMA 和树模型三类预测候选，采用只使用过去数据的滚动起点回测，并同时评价预测误差与调度费用。
- 给出 `c^T x + E[h(x,omega)]` 的分解形式以及本题变量、补救矩阵和场景块的对应关系。
- 明确区分“334 个决策日滚动求解 334 次”和“334 阶段随机规划”。
- 以两阶段随机规划或“点预测 + 安全裕度”为方案，体现计划购电与 5 倍紧急购电的不对称代价。
- 说明场景如何从历史预测残差产生，以及计划决策的非预见性。
- 解释跨日储能状态如何从 1 月 1 日的 6000 kWh 连续传递。

### 问题 3 验收

- 用滚动时域优化/MPC表达 0:00 计划与 6:00、12:00、18:00 更新。
- 已执行时段固定，未来时段只能使用当时已发布的预测。
- 对上调、下调和紧急购电费用给出可线性化表达，并标注结算歧义。
- 比较固定0:00之外 `{6,12,18}` 的全部8个子集，区分预报、重新求解和实际调电；未提供的额外官方预报只能作标注假设的仿真。
- 创新方案见 `docs/model-q3-innovation.md`：不调整区间的适用条件和推导完整，条件场景与决策导向校准可由现有附件构造，CVaR新增风险偏好单独说明；按用户要求排除鲁棒优化、DRO和Tube MPC。
- 逐项设计确定性MPC、随机MPC、费用校准和CVaR的对照；不能声称风险偏好必然降低平均费，也不能借虚构固定调整费证明事件触发优势。
- 新增独立 `docs/model-q3.tex` 与PDF：变量、目标、约束、10分钟索引、小时预报重采样、提前量指数、CVaR和实际结算完整。
- 指数尺度是待验证候选，不能冒充文献定律；对照常尺度、分箱尺度，核查R不作正确概率或均值折扣。
- 文献10—11已下载并核对相关章节；当前核心阅读为04、07、08、10、11。已有鲁棒文献仅保留索引。
- 模型数学定义与排版检查不等于数据回测验收；各数值与敏感性实验仍待后续授权。

### 问题 4 验收

- 在问题 2、3 的共同物理约束上引入波动电价；本次进一步调整依赖估计和未来条件决策结构，不改变实际更新权限。
- 区分价格信息增强参照与完整信息下界；仅预知价格或启发式参照不自动构成严格下界。
- 给出可执行的价格预测或价格场景方案，并考虑与净负荷误差的联合影响。
- 独立 `docs/model-q4.tex` 与阅读PDF包含两套模型、变量域、分解形式、结算假设和执行规则；当前只完成文档，不执行数值实验。
- 交付时段浮动结算和下单时刻解释分开；不虚构日前锁价。Q4-2是否使用0:00官方PV分开对照。
- 价格加权H函数、80%及70%—90%阈值逐项说明适用条件；风险中性边际简化不冒充全日CVaR简化。
- LP净化证明条件明确，后续用MILP核验；同次电池非预见性、日界、实际账单及冷启动可逐项检查。
- 新增公开文献12、13；明确预测与评价信息隔离、基线残差及约束防护，模型性能须由本题数据验证。
- 实质修订说明见docs/model-q4-revision-notes.md：Q4-2的rho=0/1端点、混合系数和不变边际可核对；rho只用过去验证选择，不按全年测试费用反选。
- Q4-3在0点联合制定原计划与下一更新后的条件调整；当前动作全组共同，下一节点桥接库存各组相等，同组后续动作共同。K=1应恢复旧版，K>1可嵌入旧版方案。
- 下一节点分组信号在届时确实可观测；当前仅用过去档案模拟，不能读取未来真实结果或未发布预报。组内条件H保留信息组关系。
- 文献14已在线阅读II-A、II-D并登记来源和未本地保存状态；新增结构是截断多阶段近似，不误称严格两阶段，不宣称样本外必胜。

## P5 模型评价与移交

交付物：模型检查清单、假设敏感性建议和团队分工建议，均写入现有文档。

验收标准：

- 评价指标同时覆盖预测误差和经济结果，不能只看 RMSE。
- 至少考虑效率、储能容量/功率、紧急购电倍率和预测更新频率的敏感性。
- 区分可执行策略、完美信息下界和简单基线。
- 团队成员仅凭这些文档即可开始各自学习、建模推导或后续实现，无需重新猜题意。

建议的建模协作分工：

- 成员 A：题面、附件时间索引和假设登记，负责发现口径冲突。
- 成员 B：日前预测、场景生成和预测评价，负责问题 2 的信息边界。
- 成员 C：公共储能约束、两阶段随机模型和 MPC 推导，负责问题 1 至 4 的公式一致性。
- 成员 D：文献证据、费用结算解释和交叉审阅，重点检查未来信息泄漏与量纲。

每人只在自己的文档分支修改；符号或主假设变化先在 `docs/assumptions.md` 留痕，再同步其他文件。

## 阶段状态

以下“已完成”仅指当前范围内的建模准备文档已形成，不表示数值求解或模型优越性已被验证。时间标签、结算和跨日假设仍需团队复核。

- P0：已完成，待团队评审。
- P1：已完成，待团队评审。
- P2：已完成，待团队评审。
- P3：已完成，待团队评审。
- P4：已完成，待团队评审。
- P5：已完成，待团队评审。

一个阶段只有在对应文件已经落盘、验收项能逐条核对、关键假设没有藏在文字缝隙中时才标记完成。
