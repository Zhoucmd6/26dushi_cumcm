# 第三问复现与文件指南

当前正式成果对应 `05_复核记录/pdf_aligned_20260912`。旧 `q3_20260912` 保留作为历史，当前论文不从旧目录取数。

|模块|作用|
|---|---|
|q3_forecasting.py|官方预报对齐、历史偏差与尺度、R、联合日块场景|
|q3_dispatch.py|电池互斥MILP、调整结算、CVaR、软/硬终端|
|run_q3.py|连续状态执行、原始Q及四次剩余计划留档|
|q3_workflow.py|8组校准设置、72组1月验证、B0—B4及其它对照|
|q3_diagnostics.py|提前量/发布时刻/光伏正零分组误差，R与场景截断诊断|
|audit_q3.py|不用优化器，独立核验实际轨迹、账单与预报可见性|
|prepare_q3.py|模板数据、指定日期、全部10分钟轨迹及计划版本|
|summarize_q3.py、q3_report.py|汇总对照、生成本次真实数值报告|
|q3_extra_figures.py|新增消融、尺度/R、终端与风险参数科研图|
|run_tests_q3.py、finalize_q3_records.py|实际测试记录、哈希与完整实验封存|

## 模型选择

B3主方案按1月历史费用在条件尺度候选中选择：28日窗口、3日偏差/尺度共同收缩、6小时提前量分箱、γ=0.825111111元/kWh、λ=0。尺度下限25kW、64次有放回抽样、随机种子20260912是固定实现设置。

统一尺度作为B2对照，指数尺度按PDF的历史概率评分规则被淘汰，仍保留全年比较。B4与B3使用完全相同的场景校准、抽样、负荷预测、γ与信息，仅改变α、λ。R与σ是等价表示，不乘到预测中心上。

负荷预测是PDF允许的28日历史加权均值分支，不是第二问version2的随机森林。每6小时更新电池计划，两个更新之间固定执行。论文模型描述请与这些实际选择一致。

## 从头复现

在 `03_计算工程` 打开PowerShell，设置数值库单线程，以便3个独立实验进程合理使用CPU：

```powershell
$env:PYTHONIOENCODING='utf-8'
$env:PYTHONDONTWRITEBYTECODE='1'
$env:OPENBLAS_NUM_THREADS='1'
$env:OMP_NUM_THREADS='1'
$env:MKL_NUM_THREADS='1'
& 'D:\Anaconda\python.exe' src/run_q3.py --run outputs/q3_new_run --workers 3
& 'D:\Anaconda\python.exe' src/run_tests_q3.py outputs/q3_new_run
& 'D:\Anaconda\python.exe' src/q3_diagnostics.py outputs/q3_new_run
& 'D:\Anaconda\python.exe' src/prepare_q3.py outputs/q3_new_run
& 'D:\Anaconda\python.exe' src/summarize_q3.py outputs/q3_new_run
```

`--phase calibrate`只完成热身、选参及风险验证；已有选择记录后，可用 `--phase evaluate --only main,cvar95`补算指定方案。默认运行全部实验。每14天保存断点，已完整完成的同配置实验直接复用。输入或数值代码发生变化时必须使用新运行名，程序会检查哈希，防止拼接新旧结果。

参数候选集中在 `q3_workflow.py`；该版不会自动读取并应用人工改写的configs记录文件。窗口、收缩、分箱同时用于因果校准和对应历史场景选择。主方案与各对照写入 `experiment_registry.json`。

## 提交表与图表

Excel使用已配置的本机依赖联接；更换电脑后要重新配置路径。

```powershell
./连接本机Excel依赖.ps1
& 'C:\Users\谷金龙\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' src/export_q3.mjs outputs/q3_new_run
& 'D:\Anaconda\python.exe' src/verify_q3_workbook.py outputs/q3_new_run
& 'D:\Anaconda\python.exe' src/finalize_q3_records.py outputs/q3_new_run
```

`result3.xlsx`保留模板4个工作表；计划表保存Q与原承诺费，调整表保存最终R及含紧急购电的实际总费，两张表费用不能相加。openpyxl仅用于只读逐值核验。

科研图输出7幅450dpi PNG、SVG及7页合并PDF；图表从本次结果生成。导出器的检查文件和预览放在运行档案workbook_qa中，不作为提交表。

复现结果位于新运行目录的deliverables，审计、表格逐值核对和图形检查完成后再替换04正式成果。已交付档案保留校准缓存、选择记录、全部轨迹和源码快照。`completion_record.json`记录实际实验数、独立策略数、滚动次数与20项测试结果，不把重复策略当新模型。

## 论文手使用顺序

先读公式对应说明和模型报告，再用指定日期表格、正式xlsx、全部方案对比及科研图写结果。需要保留：下调净退50%、跨日热身与年末公平库存、分箱条件尺度、概率覆盖不足、R不表示正确概率这几项说明。

统一尺度在历史验证中略便宜，不能把主B3称为全候选验证费用最优。概率评分选参和条件模型费用选参本次选中同一配置，不应虚构两者的改进差异。附件没有其他新发布时刻预报，新增时刻信息价值不能从已有四次数据直接证明。
